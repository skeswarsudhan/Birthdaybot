# Birthday Bot — Python dependencies
# Install with: pip install -r requirements.txt
# Requires Python 3.11+

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # Required for FastAPI file uploads

# Database
sqlalchemy==2.0.30

# Scheduling
apscheduler==3.10.4

# Data source
pandas==2.2.2
openpyxl==3.1.2                  # Excel read engine for pandas

# Email (smtplib, ssl, email.mime are Python standard library — no install needed)
premailer==3.10.0                 # CSS inlining for email compatibility
jinja2==3.1.4                     # HTML templating (also used by FastAPI)

# AI
groq==0.11.0                      # Groq SDK — Llama 3 via cloud inference

# Image processing
Pillow==10.3.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
pytz==2024.1
